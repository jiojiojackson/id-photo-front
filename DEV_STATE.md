# DEV_STATE

当前目标是把证件照前端部署为“提交任务与开始处理分离”的 Vercel + Vercel Queue + Neon + R2 + Lightning 架构：前端一次提交多个尺寸时为每个尺寸创建独立 Job，任务先进入队列，用户点击开始处理后再唤醒 Lightning；Lightning 端严格串行处理队列中的 Job，处理完成后结束运行，以减少按运行时间计费的 GPU 空转。当前开发分支为 `agent/queue-job-architecture`。已完成的核心内容包括 Next.js 前端、Neon/Postgres Job 数据模型调用、Vercel Queue 的名称/区域/consumer group 配置、R2 presigned URL 工具、提交任务与启动处理 API、前端队列状态和历史平均处理时间展示，以及 Lightning 调用时由 Vercel 携带平台提供的认证信息；Lightning 应用本身不实现自定义 API Key 认证。关键文件包括 `app/page.tsx`、`app/api/jobs/submit/route.ts`、`app/api/jobs/start/route.ts`、`app/api/jobs/status/route.ts`、`lib/db.ts`、`lib/queue.ts`、`lib/r2.ts` 和 `package.json`。`package.json` 当前包含 Next.js、React、Postgres 和 `@vercel/queue` 依赖；`lib/db.ts` 当前只读取 `DATABASE_URL`；`lib/queue.ts` 当前读取 `VERCEL_QUEUE_NAME`、`VERCEL_QUEUE_REGION`、`VERCEL_QUEUE_CONSUMER_GROUP`，并以 PollingQueueClient 创建队列客户端；`lib/r2.ts` 已修复 Vercel 新版 TypeScript/Web Crypto 的 BufferSource 类型问题。最近的 Vercel 部署已经验证通过编译和 TypeScript 检查，之前的 module alias、reduce 参数、`Job.unit` 和 Web Crypto 类型错误均已处理；最新失败发生在 Next.js 收集页面数据阶段，实际错误为 `DATABASE_URL is not configured`，因此当前尚未确认 Neon/Vercel Integration 实际注入的变量名称与 `lib/db.ts` 的读取名称一致。已知的架构偏差是当前 `app/api/jobs/submit/route.ts` 仍在提交任务时生成并使用 R2 PUT presigned URL 上传原图，而既定方案要求把 presigned URL 的生成推迟到用户点击“开始处理”之后以避免过期；该部分尚未完成。另一个需要继续核查的点是 `app/api/jobs/start/route.ts` 当前唤醒 Lightning 时传递 `/api/worker` bridge URL，而仓库搜索尚未确认对应的 `/api/worker` 路由存在；因此 Lightning 唤醒后的完整消费链路尚未验证。当前没有确认生产环境端到端处理成功，也没有确认 Queue、R2、Neon、Lightning 的真实运行时联调结果。下一步应先确认 Vercel/Neon Integration 实际提供的数据库环境变量名称并使 `lib/db.ts` 与其匹配，然后重新部署并读取最新 Vercel Build Log；Build 通过后继续核查 `/api/worker` 与 Queue consumer 的实际实现，修正 presigned URL 生成时机，最后进行提交 3 个 Job → 点击开始 → Lightning 唤醒 → 串行处理 → R2 结果 → 状态更新的端到端测试。